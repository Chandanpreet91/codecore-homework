<%- include("./partials/header") %>

<h1>All Cohorts</h1>

<% cohorts.forEach(cohort => { %>
  <div class="card" style="width: 18rem;">
  <img src="<%= cohort.logo_url %>" class="card-img-top" alt="<%= cohort.name %>'s cohort">
  <div class="card-body">
    <h5 class="card-title"><%= cohort.members %></h5>
    <p class="card-text"><%= cohort.name %> </p>
    <div class="d-flex">
      <a href="/cohorts/<%= cohort.id %>" class="btn btn-primary mr-2">Show</a>
      <form action="/cohorts/<%= cohort.id %>" method="POST">
        <input type="hidden" name="_method" value="DELETE" />
        <input type="submit" class="btn btn-danger" value="Delete" />
      </form>
    </div>
  </div>
  </div>
<% }) %>

<%- include("./partials/footer") %>